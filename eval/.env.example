# Committed template. Copy to `.env` and fill in — `.env` is gitignored.
#
#   cp .env.example .env
#
# Only judge/run.py reads this. The Swift evals need no key and make no network calls.

ANTHROPIC_API_KEY=

# Optional. Defaults to claude-opus-5.
# JUDGE_MODEL=claude-opus-5
